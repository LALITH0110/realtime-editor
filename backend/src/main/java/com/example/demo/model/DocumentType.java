package com.example.demo.model;

/**
 * Enum representing the different types of documents that can be created in the application.
 */
public enum DocumentType {
    code,
    word,
    presentation,
    spreadsheet,
    freeform,
    custom
} 